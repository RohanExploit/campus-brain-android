<?xml version="1.0" encoding="utf-8"?>
<!-- The question is context, not content: it recedes so the answer can lead.
     Asymmetric corner on the trailing edge anchors it to the right. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ink_input" />
    <corners
        android:topLeftRadius="@dimen/radius_md"
        android:topRightRadius="@dimen/radius_md"
        android:bottomLeftRadius="@dimen/radius_md"
        android:bottomRightRadius="@dimen/space_1" />
</shape>
